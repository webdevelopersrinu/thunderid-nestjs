# Changesets

Run `npx changeset` before you open a PR that changes behavior. It asks
what changed and how big the change is (patch/minor/major), then writes a
small file in this folder. That file becomes a line in CHANGELOG.md when
we release.

Docs: https://github.com/changesets/changesets
